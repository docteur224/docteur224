import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Espace administrateur | Docteur 224",
};

export default function LayoutAdmin({ children }: { children: React.ReactNode }) {
  return children;
}
