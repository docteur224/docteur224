import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Inscription | Docteur 224",
};

export default function LayoutInscription({ children }: { children: React.ReactNode }) {
  return children;
}
