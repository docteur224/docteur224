import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Espace assistant(e) | Docteur 224",
};

export default function LayoutAssistant({ children }: { children: React.ReactNode }) {
  return children;
}
