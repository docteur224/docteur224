import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Connexion | Docteur 224",
};

export default function LayoutConnexion({ children }: { children: React.ReactNode }) {
  return children;
}
