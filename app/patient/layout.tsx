import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Espace patient | Docteur 224",
};

export default function LayoutPatient({ children }: { children: React.ReactNode }) {
  return children;
}
