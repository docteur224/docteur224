import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Espace médecin | Docteur 224",
};

export default function LayoutMedecin({ children }: { children: React.ReactNode }) {
  return children;
}
