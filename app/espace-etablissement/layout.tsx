import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Espace établissement | Docteur 224",
};

export default function LayoutEtablissement({ children }: { children: React.ReactNode }) {
  return children;
}
